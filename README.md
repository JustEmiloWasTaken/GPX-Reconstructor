# GPX Reconstructor
Railway-ready. No Google API key. Draw a route or paste a Google Maps Directions link. Google links are converted to a road route through OSRM/OpenStreetMap, so the selected roads can differ from Google Maps.
